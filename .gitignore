# Machine-specific — vault path does not belong in a shared repo
*.code-workspace

# Playwright MCP scratch output
.playwright-mcp/

# macOS
.DS_Store

# raw scrape intermediates — data/menu.json is the shipped output
toast-*.json
comp.html
preview.html
